# =============================================================================
# Inboxorcist Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# Or run the setup script to auto-generate secrets:
#   ./scripts/setup.sh
# =============================================================================

# -----------------------------------------------------------------------------
# Google OAuth Credentials (REQUIRED)
# -----------------------------------------------------------------------------
# Get these from: https://console.cloud.google.com/apis/credentials
# 1. Create a new project (or select existing)
# 2. Enable the Gmail API
# 3. Create OAuth 2.0 Client ID credentials
# 4. Add authorized redirect URI: http://localhost:6616/auth/google/callback
#    (or https://your-domain.com/auth/google/callback for production)
#
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret

# -----------------------------------------------------------------------------
# Security Secrets (REQUIRED)
# -----------------------------------------------------------------------------
# JWT_SECRET: Used for signing authentication tokens
#   - Must be at least 32 characters
#   - Generate with: openssl rand -base64 32
#
# ENCRYPTION_KEY: Used for encrypting OAuth tokens in database
#   - Must be exactly 64 hex characters (32 bytes)
#   - Generate with: openssl rand -hex 32
#
JWT_SECRET=your-jwt-secret-at-least-32-characters-long
ENCRYPTION_KEY=your-64-character-hex-encryption-key-here-exactly

# -----------------------------------------------------------------------------
# Application URLs (OPTIONAL)
# -----------------------------------------------------------------------------
# APP_URL: Public URL where Inboxorcist is accessible
# - OAuth redirect URI is derived from this: ${APP_URL}/auth/google/callback
# - Defaults to http://localhost:3000 (dev) or http://localhost:6616 (prod)
# APP_URL=https://your-domain.com
#
# FRONTEND_URL: Where the Vite dev server runs (for CORS, dev only)
# FRONTEND_URL=http://localhost:3000

# -----------------------------------------------------------------------------
# Database Configuration (OPTIONAL)
# -----------------------------------------------------------------------------
# Docker: Uses bundled PostgreSQL by default (no config needed)
# Docker with external DB: Set DATABASE_URL and use --profile external-db
# Local development: Uses SQLite automatically (stored at ./data/inboxorcist.db)
#
# DATABASE_URL=postgres://user:password@localhost:5432/inboxorcist

# -----------------------------------------------------------------------------
# Docker Compose Settings (OPTIONAL)
# -----------------------------------------------------------------------------
# Password for Docker's bundled PostgreSQL container
# POSTGRES_PASSWORD=your_secure_postgres_password
