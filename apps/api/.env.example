# Google OAuth credentials (required)
# Get these from https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret
GOOGLE_REDIRECT_URI=http://localhost:3001/oauth/gmail/callback

# Encryption key for OAuth tokens (required)
# Generate with: openssl rand -hex 32
ENCRYPTION_KEY=your-32-byte-hex-key

# Frontend URL for redirects
FRONTEND_URL=http://localhost:3000

# Server port
PORT=3001

# Database (optional - falls back to SQLite if not set)
# DATABASE_URL=postgres://user:password@localhost:5432/inboxorcist

# Redis (optional - falls back to in-memory queue if not set)
# REDIS_URL=redis://localhost:6379
